# Homeosys evidence artifacts

Generated by `npm run evidence` / `tsx scripts/run-evidence.ts`.

## Important (observational only)

- **Stamp:** M2–M5 ladder exports associated with Reviewer **CONDITIONAL PASS**
  @ `0ac0d88`. **Engineering scaffolding ≠ scientific closure** of M2–M5.
- **M6 is hard-gated** — no autopoiesis claims or engineering in these artifacts.
- **Provisional K** (densityMin/densityMax) is unchanged from lab defaults.
- **No life / cognition / consciousness claims.** Do not treat occupancy as life.
  Conway’s Game of Life may appear only as the named cellular-automaton
  substrate in technical notes — not as product marketing.
- **P3 smoke can rewrite `evidence/`** (Developer fixing). Treat these files as
  observational snapshots until that path is hardened.

## Layout

- `m2/` — baseline vs homeostatic (shared pulse schedule)
- `m3/` — homeostatic vs ultrastable (shared sustained / nonstationary schedule); UltraEpisodeLog aggregates in summaries
- `m4/` — SetpointError vs ViabilityBand (`abControllerProtocols`)
- `m5/` — Central | Local | Coordinated (`abcOrganizationProtocols`)

Seed strategy: fixed `EVIDENCE_SEED_KEYS` list in `src/sim/evidence-matrix.ts`.

See `docs/evidence/README.md` for full reproduce notes.
